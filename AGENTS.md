# Codex Interaction Rules

- When replying with code blocks, prefer a single-line display when practical while preserving the actual content. Use inline code for short snippets, commands, paths, and key fragments to avoid taking excessive vertical space.
